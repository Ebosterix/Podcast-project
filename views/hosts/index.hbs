<h2>All hosts</h2>

{{#each allThehosts}}
        <p>{{this.name}}</p>
        <a href="/hosts/{{this._id}}">Find Out More About {{this.name}}!</a>

<form action="/hosts/{{this._id}}/delete" method="post">

  <button class="delete-button" type="delete">Delete</button>
  
</form> 

{{/each}}

